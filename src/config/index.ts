interface AppConfig {
    imagePath: string,
    canvasWidth: number,
    canvasHeight: number
}

const config: AppConfig = {
    imagePath: "./images/meme.jpg",
    canvasWidth: 800,
    canvasHeight: 450
};

export default config;

